version: '3'
services:
  backend:
    build:
      context: .
      dockerfile: docker\backend\Dockerfile
    ports:
      - "8080:80"
  frontend:
    build:
      context: .
      dockerfile: docker\frontend\Dockerfile
    ports:
      - "5173:5173"